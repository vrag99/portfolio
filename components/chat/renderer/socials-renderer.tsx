import { Social } from "@/lib/types";
import React from "react";

const SocialsRenderer = ({ socials }: { socials: Social[] }) => {
  return <div>SocialsRenderer</div>;
};

export default SocialsRenderer;
