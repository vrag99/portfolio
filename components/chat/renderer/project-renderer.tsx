import { Project } from "@/lib/types";
import React from "react";

const ProjectRenderer = ({ projects }: { projects: Project[] }) => {
  return <div>ProjectRenderer</div>;
};

export default ProjectRenderer;
