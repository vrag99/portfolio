import { Achievement } from "@/lib/types";
import React from "react";

const TimelineRenderer = ({ timeline }: { timeline: Achievement[] }) => {
  return <div>TimelineRenderer</div>;
};

export default TimelineRenderer;
